const Loading = () => (
	<div className="flex h-screen w-screen flex-col items-center justify-center">
		<div className="size-36 animate-spin rounded-full border-gray-300 border-t-2 border-r-2 border-b-2 border-l-2" />
	</div>
);

export default Loading;
